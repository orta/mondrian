const path = require("path")

module.exports = {
  config: path.resolve("src", "config", "databases.json"),
  "models-path": path.resolve("src", "models"),
  "seeders-path": path.resolve("db", "seeders"),
  "migrations-path": path.resolve("src", "migrations"),
}
